{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Solitaire",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/solitaire.exe",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "stopAtEntry": false,
      "preLaunchTask": "Build Solitaire (MSVC)"
	  
    }
  ]
}
